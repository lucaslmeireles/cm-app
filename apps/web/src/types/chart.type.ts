export type DepartmentChartType = {
  avg: string;
  department_name: string;
  metric_name: string;
};

export type EmployeeChart = {
  avg: string;
  employee_name: string;
  metric_name: string;
};

export type Chart = {
  avg: string;
  name: string;
};
